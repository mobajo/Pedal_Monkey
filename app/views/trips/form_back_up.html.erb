            <%= form_tag(trip_path(@trip), method: :patch, class: "form-inline") do %>

            <div class="form-group col-xs-12">
              <div class="text-left">
                <%= text_field_tag(:title, nil, placeholder: '"Summer holiday 2018"', class: "form-control") %>
              </div>
            </div>
            <div class="form-group col-xs-12">
              <%= submit_tag("Go ahead", class: "btn btn-success col-xs-12", style: "width: 100%;") %>
            </div>
          </div>

